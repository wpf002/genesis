/**
 * @genesis/replay — seed + configHash + paramSetId -> byte-identical state.
 *
 * Phase 1 implements reconstruction from a snapshot; Phase 8 implements the
 * permalink format that lets a third party reproduce a published run.
 */

export const REPLAY_VERSION = '0.0.0';
