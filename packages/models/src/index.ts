/**
 * @genesis/models — domain modules.
 *
 * rigor/   Phase 3–4. Calibrated population–yield–climate coupling only.
 * sandbox/ Phase 5. Everything else, every constant tagged INVENTED.
 *
 * Nothing may move from sandbox/ to rigor/ without going through calibration.
 * Module execution order is explicit and fixed — never derived from key
 * iteration over an object.
 */

export const MODULE_ORDER: readonly string[] = [];
